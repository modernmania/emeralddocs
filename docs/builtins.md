## 13. Built-ins

### Data

- `dict(k1, v1, k2, v2, ...)`
- `table(...)` (alias)
- `len(obj)`
- `append(list, item)`
- `keys(dict)`

### I/O

- `input()`
- `input(prompt)`
- `read()` / `read(prompt)`
- `plc(x)`

### Math

- `math_abs(x)`
- `math_min(a, b)`
- `math_max(a, b)`
- `math_pow(a, b)`
